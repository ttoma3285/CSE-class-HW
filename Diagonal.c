 #include <stdio.h>
#include <math.h>

int main() {
   float a,diagonal;
    printf("Enter the value of a:");
    scanf("%f",&a);
    diagonal=a* sqrt(2);
    printf("diagonal value = %.2f",diagonal);
}
